#include <iostream>
#include <vector>
using namespace std;
int main()
{
    int k;
    cin >> k;
    string s;
    char c1, c2;
    cin >> s >> c1 >> c2;
    int result = 0;
    for (int pl = 0, pr = k; pr <= s.length(); ++pl, ++pr)
    {
    }
}